# Pasal.id API Token
# 1. 到 https://pasal.id/akun 注册并创建 personal access token
# 2. 把下面一行的值替换成你的 token，保存为仓库根目录的 .env.local（ingest 脚本读取）
#    以及 apps/web/.dev.vars（若本地 preview 需要在运行时直连 API）
# 注意：.env.local 与 .dev.vars 已在 .gitignore 中，严禁提交 token。
PASAL_TOKEN=pasal_mcp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
